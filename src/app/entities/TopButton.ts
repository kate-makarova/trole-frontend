export class TopButton {
  path: string;
  class: string;
  id: string;
  name: string;

  constructor(path: string, cl: string, id: string, name: string) {
    this.path = path;
    this.class = cl;
    this.id = id;
    this.name = name;
  }
}
